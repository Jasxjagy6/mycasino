Hey man . the repository contains complete setup of telegram casino bot. i want to upgrade the bot a lot . Strict note the bot handles real money so no lazyness no code skips no stubs additions. Your one code skip or any stub will result in serious finanacial losses so be serious during the task.

Upgrades : 
Upgrade 1 :
I want to add a new surprise code drop system in the bot . The code should be dropped randomly by the bot ( ans bot should pin the surprise code drop message in the grouo when it drops the code ). Note code will be dropped as PIL image ( the text part should only conatins the status whether code is claimed or not  ) . and in the image at the top rigjr corner the bot username must be mentioned and below that Telegram casino . Note you must design a fully themed , attractive pil image that contains the instructions like surprise code drop and the big text at the centre must contains /claim <code> . Note only one user should be able to claim the code and once the code is claimed the status must be changed dynamucally in that code drops message and next to the status the user name must be mentioned who claims the code. Note the dropped code must also have wager requirements. the wager requirement will range from minimum 10$ to max 200$ in last 30 days and the selection must be random. the code mus become invalid or bot should tell the code maybe be invalid or fully claimed. Note you must design this systen very carefully and the wager requirement to claim the code should aslo be mentioned in the PIL image. The code must be dropped at random intervals and also note that the amount claimed through surprise code drops must have a 2x wager requirement ( this means that user must wager the amount at least 2 times before requesting the withdrawals. and nextkt add a new owner only command /surprisedrop off and /surprisedrop on . the working can be understood by their name. if admin uses first command than the surpruse code droppinf will be off. and to reenable it the next command can be used. design the pil image very beautifullt themed and atteactive.

Upgrade 2 :
Nextlt i want to add a new admin only command /gameshistory ( admin only strictly ) . on using this recenr games and their results along with the match id should be shown in front od admin in ( paginated form with next back inline). The recent games results must be shown first m if a game is curently active than that must ne not shown. One more thing upgrade the bot such that there are too many admin commands in the code . those commands must only responds to the admin/owner of the bot. if any other user uses that than bot should simply ignore them ( don not reply that this is owner onky command ). jjst no response shouks came for other users for all admin only commands strictly. and add a new command /admincommands ( again admin only ). on using this command bot should list all available admin command in front of admin.

Upgrade 3 :
add alternative commands to /sidebets . from now add one new command /sides . the working will be same as of /sidebets. its just another short command for /sidebets like /bal for /balance. same for /dart for /darts. just an alternative . both should work fine .

Upgrade 4 : 
Nextlt currentlt a user can challenge other user in group by using /dice amount and tagging tk his message . I want you to do a change their after player challengs 3 options came accept challenge , play with bot and cancel. i want you to remove that play with bot button ( note only when player send this /dice amount message by tagging a user message . means when challenging rhe user. if a normal /dice amount is used than the pvb button should appear. strictkt habdke it very carefullt. dont destroys or remove pvb logics or buuttons from other menu. also if a player from now onwards tag a user message and uses /dice amount XdX'w than rhe challenge must be creatw accordinf to the game mode. ( look into the code for the meaning of XdX'w ) . if the opponent confirms than bot should ask the host ( the challenge creator about the mode normal or crazy all coloured inline as they are currently and than working should remain the same. 

Upgrade 5: 
Nextlty i want to add a New option in the settings of the bot privacy mode . red inline ( if off ) and should chznge to green if a user taps on it and on it . The working will be like this that is a user privacy mode is off than the user info must ne hidden from all public results like leaderboard , highest wins , surprise code claims , etc other public information. instead of the actual usernsme bot must mentions hidden user at his place. note if the user has rank on leaderboard thsn his rank must be shown but instead of his name bot must show hidden user. and if agsin privacy mode is off than the actual details. like this the working should be . design it very carefully across all bot code.

Fix 1 :
 I also needs a fix . suppose a user creates a withdrawal rewuest than the rewuest goed to the admin with detaiks. if admin taps on aplrove and enters the txid thsn instead of sending the withdrawal processed notfiction to user bot reslonds that an error has occured. fix it properly.
 Fix 2 : 
 The games menu . currentlt the house games menu is showing two  (How? How? ) inline buttons that are non wkrking and i never tried to add them there but rhey exist. remkve these two inline from the house games section.
 Fix 3:
 
 currently the /sidebets informations is not stored in the user game history. fix this . like other game sidebets should also be shown in user game history and evry side bet must have a proper gsme id like other gsmes so that on usinf /info sidebet_id the net details csn be known. the working of the /info command will remains the same . understand it firstky first .
 
 Upgrade 6 :
 Redesign the current leaderbot and stats pil image . the current pil image are too bad . they looks to classy not beatifully , attractive , themed and designed. i want you to redesign them with proper design and text so that they looks vsry beatifull an attractive.
 
 Last strcit note . the bot handles real money so take this complete task very serioisky and conpkete it fully with full planning and understanding . and also note that applyvall the upgrades and fixes suchvthat bot performace must not decrease or bot shoukd not slows down.