# Runtime dependencies for the existing bot.py monolith.  Pinning is
# left intentionally loose so the bot can pick up patch releases; tighten
# when you cut a release.
python-telegram-bot[rate-limiter]>=20.7,<22
uvloop>=0.19.0
httpx>=0.27,<1
aiohttp>=3.9,<4
python-dotenv>=1.0.0
web3>=6.15,<8
eth-account>=0.10,<1
qrcode[pil]>=7.4
Pillow>=10.0
aiosqlite>=0.19
asyncpg>=0.29        # optional: PostgreSQL backend, falls back if unavailable
bip-utils>=2.9
openai>=1.30         # used for the Perplexity AI integration
g4f>=0.3
solders>=0.21        # optional: Solana support
solana>=0.34         # optional: Solana support
base58>=2.1
tronpy>=0.4
pytoniq-core>=0.1.36
